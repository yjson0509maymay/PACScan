streamlit>=1.54,<2
Pillow>=10.0
nibabel>=5.3,<6
numpy>=1.26,<3
scipy>=1.12,<2
pydicom>=3.0,<4
dicom2nifti>=2.6,<3
reportlab>=4.4,<5
# [2026-07-27 추가] cloud_model.py가 Hugging Face Hub에서 실제 Variant3 체크포인트를
# 내려받아 추론(CNN3D_Variant3 + Grad-CAM)하는 데 필요. CPU 전용 torch로 충분
# (Streamlit Community Cloud는 GPU 미제공, 56^3 단일 볼륨 추론이라 CPU로도 수 초 내 완료).
torch>=2.0
huggingface_hub>=0.20
# [2026-07-28 추가] 4개 모델 앙상블(CNN+ResNet+CCA+WOA) - CCA 변환기/최종 분류기가
# scikit-learn 객체(PCA/StandardScaler/GradientBoostingClassifier)를 joblib으로
# 저장한 것이라 역직렬화에 필요.
scikit-learn>=1.3
joblib>=1.3
